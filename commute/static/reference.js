window.CommuteReference = function(ctx) {
  'use strict';
  const {E,DATA,WALKS,PDF,FETCH_DATE,state,OP_NAMES,STIB_LINES,esc,safeURL,icon,realNow,dateLabel,clock,shortDate}=ctx;
  function shiftedClock(t) { const [h,m] = t.split(':').map(Number); const min = h * 60 + m + 7; return `${String(Math.floor(min / 60)).padStart(2,'0')}:${String(min % 60).padStart(2,'0')}`; }
  function shiftedClock2(t,extra) { const [h,m] = t.split(':').map(Number); const min = h * 60 + m + extra; return `${String(Math.floor(min / 60)).padStart(2,'0')}:${String(min % 60).padStart(2,'0')}`; }
  function timeChips(times, derived = false) { return `<div class="time-chips ${derived ? 'derived' : ''}">${times.map(t => `<span class="time-chip">${esc(t)}</span>`).join('')}</div>`; }
  function pdfLink() { return window.COMMUTE_SERVER ? '/shuttle.pdf' : `data:application/pdf;base64,${PDF}`; }
  function shuttleContent() {
    return `<div class="notice">${icon('shield')}<span><strong>Staff-only service.</strong> The attached 2026 PDF restricts the shuttle to employees. Home → BN includes the walk to the stop. BN → home includes the walk after the bus. Starting at TNT remains stop-to-stop. Train-platform access is excluded. All shuttle arrival times use your <strong>7-minute ride estimate</strong>.</span></div><p><strong>Your latest rules are applied:</strong> use an anchored 8-minute grid during peak windows, read the lunchtime first row as departures from TNT, and treat TNT ↔ BN as your <strong>7-minute ride in both directions</strong>. All shuttle times are <strong>black, not live</strong>. Details distinguish the assumed peak grid and user-confirmed lunch direction from published departures.</p>
      <h3>Morning · BN → TNT</h3><p>Published BN departures outside the continuous-service grid (07:00 also appears in the PDF):</p>${timeChips(E.MORNING)}
      <p style="margin-top:12px"><strong>Assumed peak departures from BN:</strong> start at 07:00 and add 8 minutes, keeping only departures inside the 07:00–09:30 window.</p>${timeChips(E.MORNING_PEAK,true)}
      <p style="margin-top:9px">The final grid departure is <strong>09:24</strong>; the next listed BN departure is <strong>09:35</strong>. The grid does not reset at each hour: 07:56 is followed by 08:04, not 08:00.</p>
      <h3>Morning return · TNT → BN</h3><p>Published BN departure +7 minutes:</p>${timeChips(E.MORNING.map(shiftedClock),true)}
      <p style="margin-top:12px"><strong>Peak TNT departures = the assumed BN grid +7 minutes:</strong></p>${timeChips(E.MORNING_PEAK.map(shiftedClock),true)}
      <p style="margin-top:9px">These run from <strong>07:07 to 09:31</strong>, followed by the derived <strong>09:42</strong> departure. Overlapping entries at 07:00 from BN / 07:07 from TNT are shown only once in the departure board.</p>
      <h3>Lunch · TNT → BN → Rogier</h3><p>You confirmed the lunchtime row starts at <strong>TNT</strong>. BN is TNT +7 minutes; Rogier comes 3 minutes after BN, both from your assumptions.</p>
      <table class="schedule-table lunch-schedule"><thead><tr><th>TNT DEPARTURE<br>CONFIRMED BY YOU</th><th>BN<br>ASSUMED +7 MIN</th><th>ROGIER<br>BN +3 MIN</th></tr></thead><tbody>${E.LUNCH.map(t => `<tr><td>${t}</td><td>${shiftedClock(t)}</td><td>${shiftedClock2(t,10)}</td></tr>`).join('')}</tbody></table>
      <p style="margin-top:10px"><strong>Lunch BN returns to TNT leave 7 minutes after the TNT start (12:12–13:57)</strong>, per your +7 rule. Rogier is continuation information only: the commute comparison still ends at BN.</p>
      <h3>Afternoon & evening · TNT → BN</h3><p>Published TNT departures:</p>${timeChips(E.AFTERNOON)}
      <p style="margin-top:12px"><strong>Assumed peak departures from TNT:</strong> start at 16:00 and add 8 minutes inside the 16:00–18:30 window.</p>${timeChips(E.AFTERNOON_PEAK,true)}
      <p style="margin-top:9px">The last grid departure is <strong>18:24</strong>; the next listed TNT departure is <strong>18:40</strong>. BN returns leave 7 minutes after each TNT start (14:12, 14:22 … 18:47), just like the morning reversed.</p>
      <h3>Last trips</h3><p>The last TNT departure is <strong>22:00</strong>; the last BN departure back to TNT is <strong>22:07</strong>. Weekends are not served and public-holiday operation is unconfirmed, so neither is ranked.</p>
      <a class="source-link" href="${pdfLink()}" download="TT-SHUTTLE-SCHEDULE.pdf">Download your original 2026 timetable ${icon('download')}</a>
      <div class="source-links"><a href="https://tour-taxis.com/your-visit/" target="_blank" rel="noopener noreferrer">[1] Official visiting information</a><a href="https://tour-taxis.com/wp-content/uploads/2026/01/TT-SHUTTLE-SCHEDULE.pdf" target="_blank" rel="noopener noreferrer">[2] Published 2026 PDF</a></div><p style="margin-top:16px">The original PDF is unchanged. Its conflicting lunch labels are resolved here by your confirmation. Exact peak-grid departures are your modelling assumption, not a new operator timetable; the PDF itself describes continuous service. Traffic and operational changes may affect actual trips.</p>`;
  }
  function sourceState(op) {
    if (!window.COMMUTE_SERVER) return '<span class="quality-pill">Offline timetable copy</span>';
    const p = state.live?.providers?.[op];
    if (E.fresh(p, realNow())) return '<span class="quality-pill live"><i class="inline-dot green"></i>Feed recently fetched</span>';
    if (p?.ok) return '<span class="quality-pill warning">Stale · not used as live</span>';
    return `<span class="quality-pill">${p?.error ? 'Live unavailable' : 'Not checked'}</span>`;
  }
  function sourcesContent() {
    const modeNotice = !window.COMMUTE_SERVER ? `<div class="notice">${icon('info')}<span><strong>This is the standalone copy.</strong> It contains real scheduled data and works without network access. Live feeds run only in the server-backed dashboard, shown in the live preview. No sample data is presented as live.</span></div>` : '';
    return modeNotice + `<p>Every departure retains its source in Details and the API. Bus routes are selected from official GTFS only when the same trip serves both your neighbourhood stop and Brussels North, in the right order, with boarding and alighting allowed.</p>` + ['stib','delijn'].map(op => {
      const d = DATA.operators[op], p = state.live?.providers?.[op];
      const lines = [...new Set(Object.values(d.routes).map(r => r.line))].sort((a,b)=>a.localeCompare(b,'en',{numeric:true}));
      const timeText = p?.fetchedAt ? `<br>Last successful live fetch: <strong>${dateLabel(p.fetchedAt,'short')}, ${clock(p.fetchedAt)}</strong> Brussels time.${p.feedTime ? `<br>Feed timestamp: ${dateLabel(p.feedTime,'short')}, ${clock(p.feedTime)}.` : '<br>STIB does not provide a generation timestamp in this endpoint; freshness is measured from fetch time.'}` : '';
      return `<section class="source-card"><div class="source-title"><h3>${OP_NAMES[op]}</h3>${sourceState(op)}</div><p><strong>Saved timetable:</strong> ${shortDate(E.dateFromKey(d.feedStart))}–${shortDate(E.dateFromKey(d.feedEnd))}.<br><strong>Direct route set:</strong> ${lines.map(esc).join(', ')}. Not every line runs at every stop, time or direction.<br><strong>Feed version:</strong> ${esc(d.feedVersion)}${op==='delijn'?`<br><strong>Live source:</strong> ${state.access?.delijnStopKeyConfigured?'De Lijn stop real-time API':'BMC GTFS-RT fallback — separate De Lijn key needed for full stop forecasts'}.`:''}${timeText}${p?.error ? `<br>${esc(p.error)}` : ''}</p><div class="source-links"><a href="${safeURL(d.sourceUrl)}" target="_blank" rel="noopener noreferrer">GTFS timetable source</a><a href="https://data.belgianmobility.io/en/data.html?agency=${op === 'stib' ? 'stibmivb' : 'delijn'}" target="_blank" rel="noopener noreferrer">Official data catalogue</a><a href="${op === 'stib' ? 'https://www.stib-mivb.be/' : 'https://www.delijn.be/'}" target="_blank" rel="noopener noreferrer">Check operator notices</a></div><small>${esc(d.attribution)}</small></section>`;
    }).join('') + `<h3>Live, without pretending</h3><ul><li><strong>Manual without a key; automatic with a key.</strong> Anonymous operator calls require a manual Refresh. With a private BMC subscription key configured on the server, or embedded in this standalone page, checks run on open/return and every minute while visible, up to 20 minutes. Hidden pages pause. Requests within 60 seconds share a cache, and the access-mode quota still applies.</li><li><strong>Two-minute freshness limit.</strong> Older predictions are discarded from comparison and the published timetable is used. The De Lijn feed timestamp is checked as well as the fetch time. STIB exposes no feed-generation timestamp here.</li><li><strong>Access-mode quota:</strong> Anonymous mode is manual-only, capped at 80 live calls per rolling day. A configured private key selects registered access with a default 10,000/day app budget, adjustable within the assigned subscription. Both modes keep the 60-second cache and 4/minute guard.${state.live?.quota ? ` Current app allowance: ${state.live.quota.refreshesLeft} two-feed refreshes remaining.` : ''}</li><li><strong>Planned trips</strong> never use today’s live predictions. GTFS weekday calendars, date exceptions and post-midnight services are respected.</li><li><strong>Not a disruption monitor.</strong> De Lijn trip cancellations / skipped stops are applied when provided. No comprehensive service-alert feed is connected; check the operators before important journeys.</li></ul><h3>Keep the schedule current</h3><p>This copy was built from feeds downloaded on ${shortDate(FETCH_DATE)}. The date coverage above is enforced; schedules are not repeated outside their validity. To refresh the source project, run <code>python refresh_schedules.py</code>, then <code>python build.py</code> and restart the server. Large full-network archives are discarded from the deliverable.</p><div class="source-links"><a href="https://data.belgianmobility.io/en/terms.html" target="_blank" rel="noopener noreferrer">BMC terms & quotas</a><a href="https://creativecommons.org/licenses/by/4.0/" target="_blank" rel="noopener noreferrer">CC BY 4.0 licence</a></div><p style="margin-top:15px">Timetables have been filtered and adapted for this personal dashboard. Sources: STIB-MIVB and De Lijn Open Data, updated ${shortDate(FETCH_DATE)}; your 2026 Tour & Taxis PDF; your 7-minute ride / morning-return rule, assumed 8-minute peak grid and lunchtime direction confirmation; your 5/5/8/11-minute walking estimates at the home end of either direction, with the original user pins and Thurn en Taxis operator coordinates. Manrope font: SIL Open Font License 1.1. No location is tracked or sent from this dashboard.</p>`;
  }
  function methodContent() {
    return `<h3>What the colours mean</h3><p><span class="tone-on-time">Green</span>: on time up to 2 minutes late. <span class="tone-minor-delay">Orange</span>: over 2 minutes and under 5 minutes late. <span class="tone-major-delay">Red</span>: 5 minutes late or more. <span class="tone-early">Blue</span>: running early. The original scheduled time stays black. Walking/home times are gray.</p><p>Delays show in whole minutes: a seconds remainder under 45 rounds down, otherwise up to the next minute; the colour always follows that displayed minute. Black means a timetable or an unknown delay—not verified on time. STIB provides expected passing times without trip IDs. Its scheduled times are matched one-to-one in time order within a 30-minute bound. The inferred original time and minute delay are estimates with a dashed underline, explained in each trip's details; they are not operator-reported delays. De Lijn deltas use exact trip/date/stop matches. Downstream arrivals projected from a live departure are estimates too.</p>
      <h3>Delay chips, collapsing and timeline</h3><p>The scheduled+delay chip appears from <strong>2 minutes</strong>; a smaller matched offset still colours the time but hides the chip. An option another listed trip beats—later start and an arrival <strong>more than 2 minutes</strong> earlier—is marked but only folds into a thin expandable separator when <strong>Collapse less-efficient options</strong> is enabled in Change; it is off by default, and the shuttle is never collapsed regardless. The <strong>Timeline</strong> view plots departures on one shared vertical clock scale: towards BN a column per stop—T&T shuttle, Picard (from home), Suzan Daniel and Thurn en Taxis—towards TNT a column per operator (TNT, MIVB, De Lijn). All columns share one compact chronological order: a later departure never appears above an earlier one (cards in different columns may overlap slightly to stay near their clock position), simultaneous departures sit at one identical height, and uncongested cards stay exactly on the shared clock scale. Cards show time, minutes-away, line and any delay chip on one line; a trip that serves both Picard and Suzan Daniel towards BN appears in both columns with a connecting line, while towards TNT the same vehicle shows just one card—to Picard where served, otherwise to Suzan Daniel.</p>
      <h3>Preferred stops; later buses remain</h3><p>From home, choose Picard over Suzan Daniel when both are available. From TNT with no walking, choose Suzan Daniel. On the return, choose Picard when the bus serves it. Line 20 still uses Suzan Daniel; 88 uses Thurn en Taxis. Exact De Lijn trips can fall back when the preferred call is skipped. STIB has no live trip IDs, so its stop preference is applied across the line rather than guessing vehicle pairings. Explicit stop filters can override this default.</p>
      <h3>Sorting</h3><p>Earliest final arrival remains primary: BN arrival outbound, home arrival on return. Equal arrivals use your order: outbound Shuttle → De Lijn → MIVB 14/20 → 88; return Shuttle → MIVB 14/20 → 88 → De Lijn. A later start breaks remaining ties. Later departures are retained.</p><p><strong>Collapsed alternatives</strong> mean another listed option lets you start strictly later and arrive strictly earlier. This does <strong>not</strong> mean cancellation. These options sit behind thin expandable rules between the other departures. Open a rule to see the comparison. Shuttles are never collapsed; the services are not cancelled. Cancellations and not-boardable trips are explicitly identified and never recommended.</p>
      <h3>Walking and trains</h3><p>Home walks: Picard 5 min, shuttle 5 min, Suzan Daniel 8 min, Thurn en Taxis 11 min. The same durations apply after alighting on the way home. A TNT start has no walking. A missing walk is never silently zero. No extra bus boarding buffer is included. Train plans reserve your adjustable 5-minute station margin and work backward from the scheduled train departure, never a delay estimate.</p>
      <h3>Occupancy</h3><p>Live occupancy is displayed only if supplied for the matching departure. The currently inspected bus feeds provide none. STIB's line-page crowding charts describe typical averages, not the approaching vehicle, so they are not presented here as live bus load.</p>`;
  }
  function stopsContent() {
    const inbound = state.direction === 'toBN';
    let content = `<div class="notice">${icon('pin')}<span>Showing stop IDs for <strong>${inbound ? 'TNT → BN' : 'BN → TNT'}</strong>. Bus boarding coordinates below come from the operators’ GTFS feeds; your walking-reference pins and the supplied TNT shuttle pin are under Walking.</span></div>`;
    for (const op of ['stib','delijn']) {
      const d = DATA.operators[op], boarding = new Set(), arriving = new Set();
      for (const leg of d.legs) {
        if (inbound !== (d.stops[leg[2]].group === 'bn')) continue;
        boarding.add(leg[1]); arriving.add(leg[2]);
      }
      content += `<h3>${OP_NAMES[op]}</h3><div class="stop-list">`;
      for (const id of [...new Set([...boarding,...arriving])].sort((a,b) => d.stops[a].group.localeCompare(d.stops[b].group))) {
        const s = d.stops[id];
        const routes = new Set();
        for (const leg of d.legs) {
          if (inbound !== (d.stops[leg[2]].group === 'bn') || (leg[1] !== id && leg[2] !== id)) continue;
          routes.add(d.routes[d.trips[leg[0]][0]].line);
        }
        content += `<div class="stop-list-item"><span class="stop-code">${esc(s.code)}</span><div><strong>${esc(s.name)}</strong><p>${boarding.has(id) ? 'Board here' : 'Get off here'} · ${[...routes].sort((a,b)=>a.localeCompare(b,'en',{numeric:true})).map(esc).join(', ')}<br>${s.lat.toFixed(6)}, ${s.lon.toFixed(6)}</p></div><a href="https://www.google.com/maps/search/?api=1&query=${s.lat},${s.lon}" target="_blank" rel="noopener noreferrer">Find stop ↗</a></div>`;
      }
      content += '</div>';
    }
    return content + `<h3>Your TNT shuttle stop</h3><div class="stop-list-item"><span class="stop-code">T&T</span><div><strong>User-provided pickup pin</strong><p>${WALKS.profiles.shuttle.lat}, ${WALKS.profiles.shuttle.lon}<br>5-minute walk from home. The same 5-minute walk is included on the way home.</p></div><a href="https://www.google.com/maps/search/?api=1&query=${WALKS.profiles.shuttle.lat},${WALKS.profiles.shuttle.lon}" target="_blank" rel="noopener noreferrer">Find stop ↗</a></div><p>The precise BN shuttle bay is still not supplied. The bus platforms above have not been overwritten with your walking-reference pins.</p><button class="source-link" style="width:100%" data-action="modal" data-tab="walking">Your walking times & reference pins ${icon('arrow-right')}</button>`;
  }
  function walkingContent() {
    return `<div class="notice">${icon('walking')}<span><strong>Walking to and from home.</strong> These are your walking estimates from Parkdreef. The same walking times are now included from the alighting stop back home on BN → TNT. From TNT itself, no walk to a stop is added.</span></div>
      <p>“Start from home” filters out buses that depart before you can walk to their stop. The board and recommendation show the latest time to leave home. If you are already at a stop, switch walking off.</p>
      <div class="walking-pin-list">${['picard','shuttle','suzan','thurn'].map(key=>{
        const p=WALKS.profiles[key];
        const location = Number.isFinite(p.lat) && Number.isFinite(p.lon) ? `<p class="coordinate-value">${p.lat}, ${p.lon}</p><a class="source-link" href="https://www.google.com/maps/search/?api=1&query=${p.lat},${p.lon}" target="_blank" rel="noopener noreferrer">Open your reference pin ${icon('arrow-up-right')}</a>` : (()=>{
          const d=DATA.operators[p.operator || 'stib'];
          const ids=[...new Set(d.legs.filter(l=>d.stops[l[1]].group===key && d.stops[l[2]].group==='bn').map(l=>l[1]))];
          return ids.map(id=>{const stop=d.stops[id];return `<p class="coordinate-value">STIB boarding stop ${esc(stop.code)} · ${stop.lat}, ${stop.lon}</p><a class="source-link" href="https://www.google.com/maps/search/?api=1&query=${stop.lat},${stop.lon}" target="_blank" rel="noopener noreferrer">Open operator boarding location ${icon('arrow-up-right')}</a>`}).join('');
        })();
        return `<section class="walking-pin-card"><div class="walking-pin-heading"><span>${icon('pin')}</span><h3>${esc(p.name)}</h3><strong>${p.minutes}<small> min walk</small></strong></div><p>${esc(p.note)}</p>${location}</section>`;
      }).join('')}</div>
      <h3>One stop name can have several platforms</h3><p>Your Suzan Daniel pin is on Picard street. You confirmed that <strong>8 minutes should apply to all nearby Suzan Daniel platforms</strong>, including De Lijn. This is an area walking estimate; each bus retains its own stop ID and actual GTFS boarding coordinates in its trip details. The same approach uses your 5-minute Picard walk for Picard boarding stops towards BN. Your TNT pin supplies the shuttle-stop location. Thurn en Taxis is a separate bus 88 stop with an 11-minute walk; its coordinates are read from STIB GTFS, not supplied as a personal pin.</p>
      <h3>How the minutes add up</h3><ol><li><strong>Ready at the stop</strong> = selected home departure time + walking time.</li><li><strong>Reachable bus</strong> = bus departure at or after that ready time, using a fresh live prediction when available.</li><li><strong>Leave home by</strong> = bus departure − walking time. No extra boarding buffer is deducted.</li><li><strong>Total time</strong> = walk + wait after the walk + ride. This equals BN arrival − selected home departure time; walking is not added to the arrival again.</li></ol><p>Example: leave home at 08:00, walk 5 minutes, board at 08:06, and arrive at 08:12. That is <strong>5 min walking + 1 min waiting + 6 min riding = 12 min</strong>. Alternatively, leave home by 08:01 for that same bus; the one-minute spare time can be spent at home.</p>
      <p style="margin-top:13px"><strong>Allow a safety margin.</strong> Walking times and live departures are estimates. The model allows arriving exactly at the departure time and adds no boarding buffer. Road crossings, slow walking or train-platform access may require extra time. No route from your home is tracked or sent to a map service.</p>`;
  }
  function keysContent() {
    const D = window.CommuteLiveDirect, status = D ? D.keyStatus() : null;
    const row = (label, masked, source) => `<div class="key-source-row"><span>${label}</span><strong>${masked || 'none'}</strong><small>${source}</small></div>`;
    const stored = status ? (status.bmcSource === 'browser storage' || status.delijnSource === 'browser storage') : false;
    return `<h3>Direct operator access</h3><p>This page can call the STIB, De Lijn and iRail APIs itself (CORS-verified gateways). Keys are resolved in this order, strongest first:</p><ol><li><strong>Browser storage</strong> — values you save on this page, kept only in this browser's localStorage for this site.</li><li><strong>Secrets file</strong> — <code>Dashboard-secrets.js</code> placed next to <code>Dashboard.html</code>; edit it to rotate keys without rebuilding.</li><li><strong>Page build</strong> — keys baked into a <code>--embed-keys</code> build.</li></ol><p>Without a key the page uses the anonymous gateway (manual Refresh, 80 calls/day). With any BMC key present, checks run automatically and the 10,000/day budget applies. A De Lijn key activates the matched stop real-time API.</p>
      ${!(window.COMMUTE_SERVER) ? `<h3>Current sources</h3>${status ? row('Belgian Mobility (STIB + De Lijn GTFS)', status.bmc, status.bmcSource) + row('De Lijn stop API', status.delijn, status.delijnSource) : '<p>The direct live layer is not available in this copy.</p>'}
      <h3>Store keys in this browser</h3><p>Values stay on this device and are sent only as subscription headers to the two operator gateways. Nothing is posted elsewhere. Use masked fields: paste once, save, done.</p>
      <div class="key-form"><label>Belgian Mobility key<input id="key-bmc" type="password" autocomplete="new-password" spellcheck="false" placeholder="32-character subscription key"></label><label>De Lijn key<input id="key-delijn" type="password" autocomplete="new-password" spellcheck="false" placeholder="separate De Lijn stop-API key"></label><div class="key-actions"><button class="primary" data-action="save-keys">Save in this browser</button><button data-action="clear-keys" ${stored ? '' : 'title="Nothing stored in this browser"'}>Remove stored keys</button></div></div>` :
      `<p class="tiny-note">This copy is served by <code>server.py</code>: keys live server-side and the browser entries above are not used here. Configure them with <code>BMC_API_KEY(_FILE)</code> / <code>DELIJN_API_KEY(_FILE)</code> on the host instead.</p>`}
      <p class="tiny-note">The secrets file is <code>Dashboard-secrets.js</code> (see <code>Dashboard-secrets.example.js</code>, git-ignored; never committed). In the anonymous sandbox or private windows, storage writes may silently fail—the page keeps working without keys.</p>`;
  }
  function technicalContent() {
    return `<h3>Live updates</h3><p>STIB WaitingTimes and De Lijn forecasts are fetched through Belgian Mobility's public API—by the Python server in server mode, or by this page itself in the standalone build (the operator gateways send CORS-permissive headers, verified 2026-09-07). Without a key, the anonymous API is manual-only: press Refresh to fetch operator data. Opening/returning can read the shared cache but makes no upstream call. With a private BMC key—server-side or embedded in this page—the page checks on open/return and once a minute while visible, for 20 minutes, pausing when hidden. The shared cache is 60 seconds; timing expires after 120 seconds. There are separate budgets: anonymous 80/day; registered defaults to 10,000/day (configure within your assigned quota). Two calls per refresh; at least 60 seconds shared cache. Other HA refresh automations share that budget. Occupancy is optional; none was present in the inspected bus feeds.</p><p>STIB has expected arrival times, not a reliable matched original departure. De Lijn has matched original/predicted times. Black/unknown is not represented as green/on time. Both operator webpage directions have been checked: STIB uses v/f, with different mappings for 88 versus 14/20; De Lijn uses its GTFS richtingCode URL.</p>
      <h3>Saved schedules</h3><pre>python refresh_schedules.py
python build.py
# Restart server, reload page.</pre><p>GTFS updates are manual or explicitly enabled on HA startup. For STIB only: <code>python refresh_schedules.py --operators stib</code>. The standalone HTML has no live API calls, but keeps timetable/manual train planning. Walking profiles and shuttle assumptions are separate from GTFS.</p>
      <h3>De Lijn stop forecasts</h3><p>A separate <code>DELIJN_API_KEY</code> / <code>DELIJN_API_KEY_FILE</code> activates the documented De Lijn multi-stop real-time API. In HA OS use masked <code>delijn_api_key</code>. It loads paired planned/actual passages for the configured corridor stops in one batch. The key is not interchangeable with BMC's key. Without it, the limited BMC GTFS-RT feed remains; it can omit Picard forecasts. Current stop-key configuration: <strong>${state.access?.delijnStopKeyConfigured?'configured':'not configured'}</strong>.</p><p>Manual refresh works with just the De Lijn key. The existing global automatic-refresh policy still requires the BMC key, so STIB's anonymous feed is never polled automatically. Each feed keeps its own budget. API requests use your keys only; no website cookies or borrowed keys are used.</p><h3>Private API key</h3><p>Configure <code>BMC_API_KEY</code> or <code>BMC_API_KEY_FILE</code> on your server. In HA OS, use the app's masked <code>bmc_api_key</code> configuration option. Leave it empty for anonymous/manual mode. Restart after changing it. Do not put the key in chat, URLs or source files. In server mode the browser receives only capability flags; it never receives the key. Authenticated requests use the registered gateway, with no anonymous fallback if the key fails. The standalone page instead resolves keys at runtime—browser storage via Reference → API keys, a git-ignored <code>Dashboard-secrets.js</code> sibling, or (legacy) a key-embedded build; see the API keys tab.</p><h3>STIB live window</h3><p>A fresh 10-minute/30-minute passing list replaces all scheduled calls through the 30-minute boundary for that boarding stop, line and direction. Timetable trips after the last prediction remain. The displayed STIB schedule/delay uses an explicit estimated match, not a provider trip ID; when data expires, normal timetable fallback applies.</p><h3>Home Assistant</h3><p><code>GET /api/next</code> and <code>/api/plan-train</code> use the same preferred-stop, walking, dominance and priority engine as this page. They read cached live data and make no upstream requests. Home arrival is a separate field; bus arrival remains the stop timestamp. Deploy on your HA OS server/LAN, not this temporary preview. No HA credentials are stored here.</p>
      <h3>Agent handoff</h3><p>Read <code>AGENTS.md</code> and the architecture, API, operations and verification files in <code>docs/</code>. The HA package still needs validation on your actual device.</p>${window.COMMUTE_SERVER?'<div class="source-links"><a href="/docs/AGENTS.md" target="_blank" rel="noopener">Agent handoff</a><a href="/docs/API.md" target="_blank" rel="noopener">API contract</a><a href="/docs/HOME_ASSISTANT.md" target="_blank" rel="noopener">HA setup</a></div>':''}`;
  }

  return {content:tab=>({shuttle:shuttleContent,sources:sourcesContent,method:methodContent,stops:stopsContent,walking:walkingContent,keys:keysContent,technical:technicalContent}[tab]||methodContent)(),pdfLink};
};
